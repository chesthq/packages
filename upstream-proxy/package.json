{
  "name": "@chest-gate/upstream-proxy",
  "version": "0.1.0",
  "description": "Templated key-holding proxy: publishers wrap third-party paid APIs without exposing keys to chest gate",
  "type": "module",
  "license": "MIT",
  "homepage": "https://chest.sh",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/smd00/chest-gate.git",
    "directory": "packages/upstream-proxy"
  },
  "keywords": ["x402", "chest", "chest-gate", "api", "monetization", "proxy", "solana"],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "chest-upstream-proxy": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "chalk": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "files": [
    "dist",
    "template",
    "README.md"
  ]
}
