{
  "name": "chesthq-packages",
  "version": "0.0.0",
  "private": true,
  "description": "Public npm packages for Chest Gate — sdk, mcp, install, upstream-proxy.",
  "workspaces": [
    "sdk",
    "mcp",
    "install",
    "upstream-proxy"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "release:sdk": "npm run build --workspace=sdk && npm publish --workspace=sdk --access public",
    "release:mcp": "npm run build --workspace=mcp && npm publish --workspace=mcp --access public",
    "release:install": "npm run build --workspace=install && npm publish --workspace=install --access public",
    "release:upstream-proxy": "npm run build --workspace=upstream-proxy && npm publish --workspace=upstream-proxy --access public"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chesthq/packages.git"
  }
}
